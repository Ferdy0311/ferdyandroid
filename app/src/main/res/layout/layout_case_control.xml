<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal" android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:layout_alignParentBottom="true"
    >


    <HorizontalScrollView
        android:id="@+id/caseNumberButtonScrollView"
        android:background="@drawable/background_placement_scrollbar_rect"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:fillViewport="false"
        android:scrollbars="none"
        android:overScrollMode="never"
        android:layout_weight="95"
        >

        <LinearLayout
            android:id="@+id/layoutCaseNumberBtnContainer"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal" >


        </LinearLayout>
    </HorizontalScrollView>

    <ImageButton
        android:layout_marginLeft="10dp"
        android:textColor="@color/bahaso_white_gray"
        android:background="@drawable/background_btn_case_check"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:paddingLeft="30dp"
        android:paddingRight="30dp"
        android:paddingTop="17dp"
        android:paddingBottom="17dp"
        android:id="@+id/btnCaseCheck"
        android:layout_weight="5"
        app:srcCompat="@drawable/ic_next"
        android:scaleType="centerInside"
        />

</LinearLayout>